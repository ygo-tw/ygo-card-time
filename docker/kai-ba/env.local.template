# 本地 Docker 測試環境變數範本
# 複製此檔案為 .env.local 並填入實際值

# 基本設定
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# 停用快取（本地測試用）
ENABLE_CACHE=false
ENABLE_REDIS_CACHE=false
ENABLE_MEMORY_CACHE=false
ENABLE_REDIS=false

# 停用速率限制（本地測試用）
RATE_LIMIT_ENABLED=false

# MongoDB 設定（請填入實際值）
ADMIN=your_mongodb_admin
PASSWORD=your_mongodb_password
DB=your_database_name

# JWT & Cookie 設定（測試用值）
JWT_SECRET=test-jwt-secret-key-please-change-in-production
COOKIE_SECRET=test-cookie-secret-key-please-change-in-production

# Response Validation
RESPONSE_VALIDATION=true
