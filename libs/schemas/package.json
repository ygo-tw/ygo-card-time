{
  "name": "@ygo/schemas",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist",
    "gen:schema": "node ./src/generate.js",
    "build": "yarn clean && yarn gen:schema && tsc"
  },
  "dependencies": {
    "json-schema-to-ts": "^3.1.0",
    "mongoose": "^8.3.4"
  },
  "devDependencies": {
    "@types/mongoose": "^5.11.97"
  }
}
